---
title: "Federation Example"
type: "concept"
importance: 5
summary: "A protocol example showing two independently authored substrates being compared through compatibility and merge reports."
relationships:
  - type: "demonstrates"
    target: "federated-knowledge-substrates"
  - type: "uses"
    target: "compatibility-report"
  - type: "uses"
    target: "merge-report"
  - type: "preserves"
    target: "independent-moderation"
---

The canonical federation example makes the protocol concrete.

It compares two independently authored substrates: one models a [knowledge substrate](/node/knowledge-substrate), the other models a memory substrate. Each keeps its own namespace and local records.

The [compatibility report](/node/compatibility-report) records how much interoperability is safe. The [merge report](/node/merge-report) records mappings and the decision not to collapse overlapping concepts into one forced truth.
